<%= form_for(@turma) do |f| %>
  <% if @turma.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@turma.errors.count, "error") %> prohibited this turma from being saved:</h2>

      <ul>
      <% @turma.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br />
    <%= f.text_field :descricao %>
  </div>
  <div class="field">
    <%= f.label 'Data Inicial' %><br />
    <%= f.date_select :dt_inicio, :order => [:day, :month, :year] %>
  </div>
  <div class="field">
    <%= f.label 'Data Final' %><br />
    <%= f.date_select :dt_fim, :order => [:day, :month, :year] %>
  </div>
  <div class="field">
    <%= f.label :limite_alunos %><br />
    <%= f.number_field :limite_alunos, :size => 3 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
